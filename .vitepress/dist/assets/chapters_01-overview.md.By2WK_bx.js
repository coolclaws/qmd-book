import{_ as t,o as d,c as o,ag as a}from"./chunks/framework.BZohXCq9.js";const m=JSON.parse('{"title":"第 1 章：项目概览","description":"","frontmatter":{},"headers":[],"relativePath":"chapters/01-overview.md","filePath":"chapters/01-overview.md"}'),r={name:"chapters/01-overview.md"};function c(s,e,i,n,l,h){return d(),o("div",null,[...e[0]||(e[0]=[a(`<h1 id="第-1-章-项目概览" tabindex="-1">第 1 章：项目概览 <a class="header-anchor" href="#第-1-章-项目概览" aria-label="Permalink to &quot;第 1 章：项目概览&quot;">​</a></h1><blockquote><p>在 AI Agent 时代，知识检索的质量直接决定了生成内容的上限。qmd 是一个完全本地运行的混合搜索引擎，专为 Markdown 文件设计，将 BM25 全文检索、向量语义搜索与 LLM 重排序三种能力融合在一个轻量级 TypeScript 工具中。它不依赖任何云端 API，所有推理均通过 node-llama-cpp 加载 GGUF 模型在本地完成——这是对&quot;数据主权&quot;与&quot;离线优先&quot;理念的一次工程实践。</p></blockquote><h2 id="为什么需要-qmd" tabindex="-1">为什么需要 qmd <a class="header-anchor" href="#为什么需要-qmd" aria-label="Permalink to &quot;为什么需要 qmd&quot;">​</a></h2><h3 id="markdown-知识库的检索困境" tabindex="-1">Markdown 知识库的检索困境 <a class="header-anchor" href="#markdown-知识库的检索困境" aria-label="Permalink to &quot;Markdown 知识库的检索困境&quot;">​</a></h3><p>当你积累了数百甚至数千份 Markdown 笔记时，操作系统自带的搜索工具很快就会力不从心。<code>grep</code> 只能做字面匹配，无法理解&quot;机器学习&quot;和&quot;深度学习&quot;之间的语义关联。而云端搜索服务虽然强大，却意味着你的私人笔记需要上传到第三方服务器。</p><p>qmd 的作者 Tobi Lutke——Shopify 的 CEO——正是在这样的场景下创建了这个项目。作为一名重度 Markdown 用户和 AI 工具实践者，他需要一个能够在本地高效检索个人知识库的方案，同时要能作为 AI Agent 的工具无缝集成。</p><h3 id="三重搜索的设计哲学" tabindex="-1">三重搜索的设计哲学 <a class="header-anchor" href="#三重搜索的设计哲学" aria-label="Permalink to &quot;三重搜索的设计哲学&quot;">​</a></h3><p>qmd 的核心理念是&quot;混合搜索&quot;（Hybrid Search），它将三种互补的检索策略组合在一起：</p><table tabindex="0"><thead><tr><th>搜索策略</th><th>技术实现</th><th>优势</th><th>局限</th></tr></thead><tbody><tr><td>BM25 全文检索</td><td>SQLite FTS5</td><td>精确关键词匹配，速度极快</td><td>无法理解语义相似性</td></tr><tr><td>向量语义搜索</td><td>sqlite-vec + 嵌入模型</td><td>捕捉语义关联</td><td>可能遗漏精确术语</td></tr><tr><td>LLM 重排序</td><td>node-llama-cpp 重排序模型</td><td>综合理解查询意图</td><td>计算成本较高</td></tr></tbody></table><p>这三层机制在 <code>src/store.ts</code> 中通过 Reciprocal Rank Fusion（RRF）算法进行融合，最终返回兼顾精确性与语义相关性的搜索结果。</p><h2 id="核心架构" tabindex="-1">核心架构 <a class="header-anchor" href="#核心架构" aria-label="Permalink to &quot;核心架构&quot;">​</a></h2><h3 id="数据存储层-sqlite-的极致运用" tabindex="-1">数据存储层：SQLite 的极致运用 <a class="header-anchor" href="#数据存储层-sqlite-的极致运用" aria-label="Permalink to &quot;数据存储层：SQLite 的极致运用&quot;">​</a></h3><p>qmd 的存储层完全构建在 SQLite 之上，但并非简单地使用一张表。它巧妙地利用了 SQLite 的扩展生态，在单一数据库文件中同时实现关系型存储、全文索引和向量检索。</p><p>在 <code>src/store.ts</code> 的第 709-764 行，定义了核心数据表结构。<code>content</code> 表（第 709 行）以内容哈希为主键存储文档原文，实现了内容去重。<code>documents</code> 表（第 714 行）记录文件的元数据——路径、标题、所属集合和活跃状态。</p><p>全文搜索依赖 FTS5 虚拟表（第 764 行），使用 <code>porter unicode61</code> 分词器，同时支持词干提取和 Unicode 文本：</p><div class="language-sql vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">CREATE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> VIRTUAL </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">TABLE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> documents_fts </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">USING</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> fts5(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  filepath, title, body, tokenize</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;porter unicode61&#39;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>向量搜索则通过 sqlite-vec 扩展实现，在第 1644 行创建 <code>vectors_vec</code> 虚拟表，使用余弦距离度量。每个文档被分块后生成嵌入向量，存储在 <code>content_vectors</code> 表（第 746 行）中。</p><h3 id="搜索引擎层-从查询到结果" tabindex="-1">搜索引擎层：从查询到结果 <a class="header-anchor" href="#搜索引擎层-从查询到结果" aria-label="Permalink to &quot;搜索引擎层：从查询到结果&quot;">​</a></h3><p>当用户发起一次搜索时，qmd 的处理流程分为以下几个阶段。</p><p><strong>BM25 检索</strong>在 <code>searchFTS()</code> 函数（第 2294 行）中实现。它使用 FTS5 的内置 BM25 评分函数，权重配置为 <code>bm25(documents_fts, 10.0, 1.0)</code>——文件路径的权重为 10.0，正文为 1.0。BM25 返回的负分数通过 <code>|score|/(1+|score|)</code> 归一化到 [0, 1) 区间。</p><p><strong>向量检索</strong>在 <code>searchVec()</code> 函数（第 2346 行）中实现。它采用两步策略：先在 sqlite-vec 虚拟表中执行纯向量查询（避免 JOIN 操作的性能开销），再通过哈希关联回文档表。最终得分为 <code>1 - cosine_distance</code>。对于同一文件的多个分块命中，只保留距离最近的那个。</p><p><strong>RRF 融合</strong>是混合搜索的关键一步。Reciprocal Rank Fusion 算法将 BM25 和向量搜索的排名列表合并，公式为 <code>1/(k + rank)</code>，其中 k 是平滑参数。这种方法的优点是不依赖绝对分数，只关注相对排名，因此能自然地平衡两种异构检索结果。</p><h3 id="llm-层-本地推理引擎" tabindex="-1">LLM 层：本地推理引擎 <a class="header-anchor" href="#llm-层-本地推理引擎" aria-label="Permalink to &quot;LLM 层：本地推理引擎&quot;">​</a></h3><p><code>src/llm.ts</code> 中的 <code>LlamaCpp</code> 类（第 235 行）封装了所有与本地大模型的交互。qmd 默认配置了三个 GGUF 模型，各司其职：</p><table tabindex="0"><thead><tr><th>模型</th><th>用途</th><th>文件</th></tr></thead><tbody><tr><td>embeddinggemma 300M</td><td>文档嵌入</td><td><code>embeddinggemma-300M-Q8_0.gguf</code></td></tr><tr><td>Qwen3-Reranker 0.6B</td><td>结果重排序</td><td><code>qwen3-reranker-0.6b-q8_0.gguf</code></td></tr><tr><td>qmd-query-expansion 1.7B</td><td>查询扩展</td><td><code>qmd-query-expansion-1.7B-q4_k_m.gguf</code></td></tr></tbody></table><p>模型通过 HuggingFace URI 格式引用（如 <code>hf:ggml-org/embeddinggemma-300M-GGUF/...</code>），首次使用时自动下载到 <code>~/.cache/qmd/models</code> 目录。<code>LlamaCpp</code> 类实现了惰性加载和 5 分钟不活跃自动卸载（第 255 行），避免常驻内存占用。</p><p>批量嵌入通过 <code>embedBatch()</code> 方法（第 525 行）实现，它根据可用 VRAM 的 25% 或 CPU 核心数自动分配并行上下文数量。</p><h3 id="文档分块策略" tabindex="-1">文档分块策略 <a class="header-anchor" href="#文档分块策略" aria-label="Permalink to &quot;文档分块策略&quot;">​</a></h3><p>好的分块直接影响检索质量。<code>src/store.ts</code> 中实现了两种分块策略：基于字符的 <code>chunkDocument()</code>（第 2209 行）和基于 Token 的 <code>chunkDocumentByTokens()</code>（第 2234 行）。</p><p>核心参数配置如下：目标分块大小为 900 tokens，重叠区域为 135 tokens（15%），搜索窗口为 200 tokens。系统在窗口内扫描最佳切割点，优先在 Markdown 标题处断开（H1 得分 100，H2-H6 得分 90-50），其次是代码块边界（得分 80）和段落分隔符（得分 20）。</p><p><code>findBestCutoff()</code> 函数（第 384 行）使用平方衰减函数 <code>1.0 - (distance/window)^2 * 0.7</code> 来平衡切割点质量与位置，确保即使较远的标题断点也能胜过近处的低质量断点。</p><h2 id="三种消费模式" tabindex="-1">三种消费模式 <a class="header-anchor" href="#三种消费模式" aria-label="Permalink to &quot;三种消费模式&quot;">​</a></h2><p>qmd 提供了三种使用方式，它们共享同一个 <code>Store</code> 核心。</p><p><strong>命令行工具</strong>（<code>src/cli/qmd.ts</code>）是最直接的交互方式。它提供了 <code>query</code>、<code>search</code>、<code>vsearch</code>、<code>get</code>、<code>multi-get</code> 等二十余个子命令，支持 JSON、CSV、XML、Markdown 等多种输出格式。</p><p><strong>SDK 库</strong>（<code>src/index.ts</code>）导出 <code>createStore()</code> 工厂函数，返回一个 <code>QMDStore</code> 接口，包含 <code>search()</code>、<code>searchLex()</code>、<code>searchVector()</code>、<code>get()</code>、<code>multiGet()</code> 等方法，供 Node.js / Bun 应用程序直接调用。</p><p><strong>MCP 服务器</strong>（<code>src/mcp/server.ts</code>）实现了 Model Context Protocol，提供 <code>query</code>、<code>get</code>、<code>multi_get</code>、<code>status</code> 四个工具，支持 stdio 和 HTTP 两种传输模式。这使得 qmd 可以作为 Claude、ChatGPT 等 AI Agent 的知识检索后端。</p><p>三种模式的架构关系可以概括为：用户请求通过 CLI / SDK / MCP 进入系统，统一调用 Store 层完成索引和检索，Store 层与 SQLite 数据库（FTS5 + sqlite-vec）和 LLM 引擎（node-llama-cpp + GGUF 模型）交互，最终返回排序后的结果。</p><h2 id="关键依赖一览" tabindex="-1">关键依赖一览 <a class="header-anchor" href="#关键依赖一览" aria-label="Permalink to &quot;关键依赖一览&quot;">​</a></h2><table tabindex="0"><thead><tr><th>依赖包</th><th>版本</th><th>职责</th></tr></thead><tbody><tr><td>better-sqlite3</td><td>^12.4.5</td><td>Node.js 环境下的 SQLite 驱动</td></tr><tr><td>node-llama-cpp</td><td>^3.17.1</td><td>本地 GGUF 模型推理运行时</td></tr><tr><td>sqlite-vec</td><td>^0.1.7-alpha.2</td><td>SQLite 向量搜索扩展</td></tr><tr><td>@modelcontextprotocol/sdk</td><td>^1.25.1</td><td>MCP 协议实现</td></tr><tr><td>fast-glob</td><td>^3.3.0</td><td>高性能文件路径匹配</td></tr><tr><td>picomatch</td><td>^4.0.0</td><td>Glob 模式匹配库</td></tr><tr><td>zod</td><td>4.2.1</td><td>运行时类型校验</td></tr><tr><td>yaml</td><td>^2.8.2</td><td>集合配置文件解析</td></tr></tbody></table><p>值得注意的是，sqlite-vec 还提供了按平台分发的可选依赖（darwin-arm64、darwin-x64、linux-arm64 等），确保原生扩展在不同操作系统上正确加载。</p><h2 id="本章小结" tabindex="-1">本章小结 <a class="header-anchor" href="#本章小结" aria-label="Permalink to &quot;本章小结&quot;">​</a></h2><p>qmd 是一个为 AI Agent 时代设计的本地混合搜索引擎。它在单一 SQLite 数据库中集成了 BM25 全文检索（FTS5）、向量语义搜索（sqlite-vec）和 LLM 重排序三重机制，通过 RRF 算法融合结果。所有推理通过 node-llama-cpp 在本地完成，无需云端依赖。项目约 12,000 行 TypeScript 代码，核心逻辑集中在 <code>src/store.ts</code>（搜索与索引）和 <code>src/llm.ts</code>（模型管理）两个文件中，通过 CLI、SDK 和 MCP 三种模式对外提供服务。这种&quot;重存储层、轻接口层&quot;的设计，使得同一套搜索原语能够灵活适配不同的使用场景。</p>`,42)])])}const u=t(r,[["render",c]]);export{m as __pageData,u as default};
